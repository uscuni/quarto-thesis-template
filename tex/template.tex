$passoptions.latex()$
\documentclass[
$for(babel-otherlangs)$
  $babel-otherlangs$,
$endfor$
$if(babel-lang)$
  $babel-lang$,
$endif$
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}
$if(beamerarticle)$
\usepackage{beamerarticle} % needs to be loaded first
$endif$
\usepackage{xcolor}
\usepackage{etoolbox}
$if(geometry)$
\usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}
$endif$
\usepackage{amsmath,amssymb}
$if(cancel)$
\usepackage{cancel}
$endif$
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$
$fonts.latex()$
$font-settings.latex()$
$common.latex()$
$after-header-includes.latex()$
$hypersetup.latex()$
$if(pdf-trailer-id)$

\ifXeTeX
\special{pdf:trailerid [ $pdf-trailer-id$ ]}
\fi
\ifPDFTeX
\pdftrailerid{}
\pdftrailer{/ID [ $pdf-trailer-id$ ]}
\fi
\ifLuaTeX
\pdfvariable trailerid {[ $pdf-trailer-id$ ]}
\fi
$endif$

\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$date$}

\newcommand{\thesislang}{$if(lang)$$lang$$else$cs$endif$}
\newif\ifthesisenglish
\ifdefstring{\thesislang}{en}{\thesisenglishtrue}{\thesisenglishfalse}
\newcommand{\thesislabel}[2]{\ifthesisenglish#2\else#1\fi}
\newcommand{\thesisstudyprogramlabel}{\thesislabel{Studijní program}{Study programme}}
\newcommand{\thesissupervisorlabel}{\thesislabel{Školitel}{Supervisor}}
\newcommand{\thesisabstractlabel}{\thesislabel{Abstrakt}{Abstract}}
\newcommand{\thesiskeywordslabel}{\thesislabel{Klíčová slova}{Keywords}}
\newcommand{\thesisotherabstractlabel}{\thesislabel{Abstract}{Abstrakt}}
\newcommand{\thesisotherkeywordslabel}{\thesislabel{Keywords}{Klíčová slova}}
\newcommand{\thesisabbreviationslabel}{\thesislabel{Seznam zkratek}{List of abbreviations}}

\begin{document}
$if(has-frontmatter)$
\frontmatter
$endif$

\setstretch{1.5}

\begin{titlepage}
\centering
\textbf{$thesis.university$}\\
\textbf{$thesis.faculty$}\\[10mm]

\thesisstudyprogramlabel: $thesis.study_program$ \break

\begin{figure}[h!]
\begin{center}
\includegraphics[height=$if(thesis.logo_height)$$thesis.logo_height$$else$8cm$endif$]{$thesis.logo$}
\end{center}
\end{figure}

\textbf{$for(author)$$author$$sep$, $endfor$}\\[1cm]

$title$\\[5mm]
$thesis.title_en$\\[1cm]

$thesis.thesis_type$\\[1cm]

\thesissupervisorlabel: \textbf{$thesis.supervisor$}\\
\null\vfill

$thesis.place$, $if(thesis.year)$$thesis.year$$else$\the\year$endif$
\end{titlepage}

\pagestyle{empty}
\null\vfill
\noindent $thesis.declaration$
\\[2cm]
$thesis.place$, \mydate\today \hfill $thesis.signature$
\newpage

$if(thesis.ai_declaration)$
\null\vfill
\noindent $thesis.ai_declaration$
\newpage
$endif$

\null\vfill
\noindent $thesis.acknowledgement$
\newpage

\section*{\thesisabstractlabel}
\ifthesisenglish
$thesis.abstract_en$
\else
$thesis.abstract_cs$
\fi

\section*{\thesiskeywordslabel}
\ifthesisenglish
$thesis.keywords_en$
\else
$thesis.keywords_cs$
\fi
\\

\section*{\thesisotherabstractlabel}
\ifthesisenglish
$thesis.abstract_cs$
\else
$thesis.abstract_en$
\fi

\section*{\thesisotherkeywordslabel}
\ifthesisenglish
$thesis.keywords_cs$
\else
$thesis.keywords_en$
\fi

\newpage

\section*{\thesisabbreviationslabel}
$for(thesis.abbreviations)$
$thesis.abbreviations.term$ - $thesis.abbreviations.description$$sep$\\
$endfor$

\newpage

\tableofcontents
\newpage

\pagestyle{plain}
\setstretch{1.5}

$for(include-before)$
$include-before$

$endfor$
$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
{
$if(colorlinks)$
$if(toccolor)$
\hypersetup{linkcolor=$toccolor$}
$endif$
$endif$
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$
$if(lof)$
\listoffigures
$endif$
$if(lot)$
\listoftables
$endif$
$if(linestretch)$
\setstretch{$linestretch$}
$endif$
$if(has-frontmatter)$
\mainmatter
$endif$
$body$

$if(has-frontmatter)$
\backmatter
$endif$
$for(include-after)$
$include-after$

$endfor$
\end{document}
